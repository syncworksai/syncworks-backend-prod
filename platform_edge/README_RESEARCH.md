# EDGE Research Backtest

Replays completed MLB game states to test the experimental model and the comeback hypothesis. This pass does not use historical Kalshi prices, so it is a calibration study rather than a profitability study.

Authenticated endpoint: `GET /api/v1/edge/research/mlb/backtest/?days=14&max_games=100`.
